const player = (initial, preferenceBefore) => ({ initial, preferenceBefore })

const players = [
	player('DD', true),
	player('DM', true),
	player('JC', true),
	player('JK', true),
	player('ND', true),
	player('PC', true),
	player('MS', true),
	player('SC', true)
]

console.log(players)